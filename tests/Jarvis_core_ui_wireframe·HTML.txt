<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>JARVIS — Core Interface Wireframe</title>
<style>
  :root{
    --bg:#05070a;
    --bg2:#0a0f16;
    --cyan:#3fd0ff;
    --cyan-dim:#1a5f78;
    --amber:#ffb545;
    --red:#ff4d4d;
    --text:#cfe9f5;
    --text-dim:#5f7f8f;
    --line: rgba(63,208,255,0.18);
  }
  *{box-sizing:border-box;margin:0;padding:0;}
  html,body{height:100%;}
  body{
    background: radial-gradient(1200px 600px at 50% 20%, #0c1620 0%, #05070a 60%), var(--bg);
    color:var(--text);
    font-family: 'Segoe UI', system-ui, sans-serif;
    overflow:hidden;
    height:100vh;
  }
  .bg-grid{
    position:fixed; inset:0;
    background-image:
      linear-gradient(var(--line) 1px, transparent 1px),
      linear-gradient(90deg, var(--line) 1px, transparent 1px);
    background-size: 42px 42px;
    opacity:0.25;
    mask-image: radial-gradient(ellipse at 50% 40%, black 10%, transparent 70%);
  }

  /* ===== Top bar ===== */
  .topbar{
    position:relative; z-index:5;
    display:flex; align-items:center; justify-content:space-between;
    padding:16px 28px;
    border-bottom:1px solid var(--line);
    backdrop-filter: blur(4px);
  }
  .brand{display:flex; align-items:baseline; gap:10px;}
  .brand .mark{
    width:30px;height:30px;border-radius:8px;
    background:linear-gradient(135deg,var(--cyan),var(--cyan-dim));
    display:flex;align-items:center;justify-content:center;
    font-weight:800;color:#02121a;font-size:15px;
  }
  .brand .name{font-weight:700; letter-spacing:2px; font-size:15px;}
  .brand .name b{color:var(--cyan);}
  .brand .sub{font-size:10px; letter-spacing:2px; color:var(--text-dim); margin-left:8px;}
  .pills{display:flex; gap:10px;}
  .pill{
    display:flex; align-items:center; gap:6px;
    font-size:10px; letter-spacing:1px; color:var(--text-dim);
    border:1px solid var(--line); padding:5px 10px; border-radius:20px;
  }
  .dot{width:6px;height:6px;border-radius:50%; background:var(--cyan); box-shadow:0 0 8px var(--cyan);}
  .dot.amber{background:var(--amber); box-shadow:0 0 8px var(--amber);}

  /* ===== Layout ===== */
  .shell{ position:relative; z-index:2; display:grid; grid-template-columns: 64px 1fr 300px; height:calc(100vh - 63px);}

  /* Left icon rail (kept from existing app) */
  .rail{border-right:1px solid var(--line); display:flex; flex-direction:column; align-items:center; padding:18px 0; gap:22px;}
  .rail .r-btn{width:34px;height:34px;border-radius:9px;border:1px solid var(--line); color:var(--text-dim); display:flex;align-items:center;justify-content:center; font-size:13px;}
  .rail .r-btn.active{color:var(--cyan); border-color:var(--cyan); box-shadow:0 0 12px rgba(63,208,255,0.35);}

  /* ===== Center: the holographic core ===== */
  .core-stage{ position:relative; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:26px; }

  .core-wrap{ position:relative; width:380px; height:380px; }
  .ring{ position:absolute; inset:0; border-radius:50%; border:1px solid var(--cyan-dim); opacity:0.6; }
  .ring.r1{ animation: spin 18s linear infinite; border-style:dashed; }
  .ring.r2{ inset:34px; animation: spin 26s linear infinite reverse; }
  .ring.r3{ inset:68px; border-color:var(--cyan); opacity:0.35; animation: spin 12s linear infinite; }
  .ring.r3::before, .ring.r3::after{
    content:""; position:absolute; width:6px; height:6px; border-radius:50%;
    background:var(--cyan); box-shadow:0 0 10px var(--cyan);
    top:-3px; left:calc(50% - 3px);
  }
  .ring.r3::after{ top:auto; bottom:-3px; }

  .core-orb{
    position:absolute; inset:118px; border-radius:50%;
    background: radial-gradient(circle at 40% 35%, #a8f1ff 0%, var(--cyan) 35%, #0d3a4a 75%, transparent 100%);
    box-shadow: 0 0 60px rgba(63,208,255,0.55), 0 0 120px rgba(63,208,255,0.25) inset;
    animation: pulse 2.6s ease-in-out infinite;
  }
  .core-label{
    position:absolute; inset:0; display:flex; align-items:center; justify-content:center;
    flex-direction:column; text-align:center;
  }
  .core-label .state{ font-size:12px; letter-spacing:5px; color:#02121a; font-weight:800; }
  .core-label .sub{ font-size:9px; letter-spacing:2px; color:#0a2733; margin-top:3px; }

  @keyframes spin{ to{ transform: rotate(360deg);} }
  @keyframes pulse{ 0%,100%{ transform:scale(1); } 50%{ transform:scale(1.045);} }

  .status-line{ font-size:12px; letter-spacing:4px; color:var(--cyan); text-transform:uppercase; }
  .status-line .caret{ opacity:0.5; animation: blink 1.1s steps(2) infinite; }
  @keyframes blink{ 50%{opacity:0;} }

  .mic-row{ display:flex; gap:14px; }
  .mic-btn{
    width:56px;height:56px;border-radius:50%; border:1px solid var(--line);
    display:flex;align-items:center;justify-content:center; color:var(--cyan); font-size:18px;
    background:rgba(63,208,255,0.05);
  }
  .mic-btn.main{ width:64px;height:64px; border-color:var(--cyan); box-shadow:0 0 20px rgba(63,208,255,0.4); }

  .transcript{
    width:520px; max-width:80vw; text-align:center; color:var(--text-dim); font-size:13px; line-height:1.6; min-height:40px;
  }
  .transcript b{color:var(--text); font-weight:600;}

  /* ===== Right: module / task panel ===== */
  .side{ border-left:1px solid var(--line); padding:20px; overflow-y:auto; }
  .side h3{ font-size:11px; letter-spacing:2px; color:var(--text-dim); margin-bottom:14px; }
  .mod{ display:flex; align-items:center; gap:10px; padding:10px 8px; border-radius:8px; margin-bottom:6px; border:1px solid transparent; }
  .mod.on{ border-color:var(--line); background:rgba(63,208,255,0.04); }
  .mod .m-dot{ width:7px;height:7px;border-radius:50%; background:var(--cyan); box-shadow:0 0 6px var(--cyan); }
  .mod .m-dot.off{ background:#3a4b52; box-shadow:none; }
  .mod .m-name{ font-size:12.5px; }
  .mod .m-desc{ font-size:10.5px; color:var(--text-dim); }

  .task-card{ margin-top:22px; border:1px solid var(--line); border-radius:10px; padding:12px; }
  .task-card .t-head{ display:flex; justify-content:space-between; font-size:10px; letter-spacing:1px; color:var(--text-dim); margin-bottom:8px; }
  .task-card .t-row{ display:flex; align-items:center; gap:8px; font-size:11.5px; padding:4px 0; color:var(--text); }
  .badge{ font-size:9px; padding:2px 6px; border-radius:5px; border:1px solid var(--line); color:var(--text-dim); }

  .footnote{ position:fixed; bottom:10px; left:0; right:0; text-align:center; font-size:10px; color:#33474f; letter-spacing:1px; }
</style>
</head>
<body>

<div class="bg-grid"></div>

<div class="topbar">
  <div class="brand">
    <div class="mark">J</div>
    <div class="name">JAR<b>VIS</b></div>
    <div class="sub">PRADEEP CONTENT EMPIRE</div>
  </div>
  <div class="pills">
    <div class="pill"><div class="dot"></div>GROQ · ONLINE</div>
    <div class="pill"><div class="dot amber"></div>BRIDGE · 8765</div>
    <div class="pill"><div class="dot"></div>MIC · READY</div>
  </div>
</div>

<div class="shell">

  <div class="rail">
    <div class="r-btn active">◎</div>
    <div class="r-btn">🌐</div>
    <div class="r-btn">▶</div>
    <div class="r-btn">⌂</div>
    <div class="r-btn">⚙</div>
  </div>

  <div class="core-stage">
    <div class="core-wrap">
      <div class="ring r1"></div>
      <div class="ring r2"></div>
      <div class="ring r3"></div>
      <div class="core-orb">
        <div class="core-label">
          <div class="state">SYSTEMS ONLINE</div>
          <div class="sub">V4 · PHASE 2 · BRAIN ACTIVE</div>
        </div>
      </div>
    </div>

    <div class="status-line">LISTENING<span class="caret">_</span></div>

    <div class="transcript">
      <b>Boss:</b> "Jarvis, check today's Instagram uploads for Darkfarts."<br>
      Routed → <b>automation.instagram</b> · groq/llama-3.1-8b-instant
    </div>

    <div class="mic-row">
      <div class="mic-btn">⌘</div>
      <div class="mic-btn main">🎙</div>
      <div class="mic-btn">⏻</div>
    </div>
  </div>

  <div class="side">
    <h3>ACTIVE MODULES</h3>
    <div class="mod on"><div class="m-dot"></div><div><div class="m-name">AI Chat</div><div class="m-desc">Groq llama-3.1-8b-instant</div></div></div>
    <div class="mod on"><div class="m-dot"></div><div><div class="m-name">Voice I/O</div><div class="m-desc">hi-IN listen · TTS</div></div></div>
    <div class="mod on"><div class="m-dot"></div><div><div class="m-name">Memory</div><div class="m-desc">Redis + long_memory</div></div></div>
    <div class="mod"><div class="m-dot off"></div><div><div class="m-name">PC Control</div><div class="m-desc">idle</div></div></div>
    <div class="mod"><div class="m-dot off"></div><div><div class="m-name">World Monitor</div><div class="m-desc">idle</div></div></div>

    <div class="task-card">
      <div class="t-head"><span>LIVE TASKS</span><span class="badge">3</span></div>
      <div class="t-row">🟢 Lumix post scheduler — running</div>
      <div class="t-row">🟡 Darkfarts video render — queued</div>
      <div class="t-row">⚪ Client site audit — idle</div>
    </div>
  </div>

</div>

<div class="footnote">WIREFRAME — visual reference only, not wired to agent.py yet</div>

</body>
</html>